import React from 'react';
import PropTypes from 'prop-types';

const Main = props => (
  <section className="padding--bottom-3">
    <article
      {...props}
    />
  </section>
);

export { Main };
